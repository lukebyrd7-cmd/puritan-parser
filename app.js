// Compatibility shim: the app state moved to src/app-state.js for the Phase 1 modular entry point.
// index.html now loads src/main.js, which loads src/app-state.js and the focused modules in order.
